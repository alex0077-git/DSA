class Solution:
    def maxJump(self, stones: List[int]) -> int:
        n=len(stones)
        if n==2:
            return stones[1]-stones[0]
        maxx=0
        for i in range(n-2):
            jump=stones[i+2]-stones[i]
            maxx=max(maxx,jump)
        return maxx
