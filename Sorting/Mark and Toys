def maximumToys(prices, k):
    # Write your code here
    prices.sort()
    count=0
    for i in range(len(prices)):
        if prices[i]<k:
            k-=prices[i]
            count+=1
    return count
