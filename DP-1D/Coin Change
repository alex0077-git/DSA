class Solution:
    def coinChange(self, arr,k):
        dp=[k+1]*(k+1)
        dp[0]=0
        for i in range (1,k+1):
            for coin in arr:
                if coin<=i:
                    dp[i]=min(dp[i],dp[i-coin]+1)
        return dp[k] if dp[k]!=k+1 else -1      
