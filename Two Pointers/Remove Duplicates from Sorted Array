class Solution:
    def removeDuplicates(self, arr: List[int]) -> int:
        if not arr:
            return 0
        i=0
        for j in range(len(arr)):
            if arr[i]!=arr[j]:
                i+=1
                arr[i]=arr[j]
        return i+1
