def makeAnagram(a, b):
    # Write your code here
    count=0
    d=set(a+b)
    for char in d:
        counta=a.count(char)
        countb=b.count(char)

        count+=abs(counta-countb)
    return count
