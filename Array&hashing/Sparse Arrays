def matchingStrings(stringList, queries):
    # Write your code here
    res=[]
    for i in queries:
        count=0
        for j in stringList:
            if i==j:
                count+=1
        res.append(count)
    return res
