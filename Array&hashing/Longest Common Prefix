class Solution:
    def longestCommonPrefix(self, strs: list[str]) -> str:
        result=""
        first=min(strs)
        last=max(strs)
        for i in range(min(len(first),len(last))):
            if first[i]!=last[i]:
                return result
            result+=first[i]
        return result

