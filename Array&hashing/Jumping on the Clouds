def jumpingOnClouds(c):
    # Write your code here
    n=len(c)
    current=0
    count=0
    while current<n-1:
        if current+2<n and c[current+2]==1:
            current+=1
            count+=1
        else:
            current+=2
            count+=1
    return count
