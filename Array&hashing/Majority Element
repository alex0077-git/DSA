class Solution:
    def majorityElement(self, nums: List[int]) -> int:
        f=0
        a=0
        for i in nums:
            if f==0:
                a=i
            if i!=a:
                f-=1
            else:
                f+=1
        return a


        
